


const Home = () => {
    // const authInfo=useContext(AuthContext);
    // console.log(authInfo); 
    return (
        <div>
            <h1 className="text-3xl">Home:</h1>
            
        </div>
    );
};

export default Home;